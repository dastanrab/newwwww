/** Mock profile for UI only; replace with API data later. */
export const MOCK_DRIVER_DISPLAY = {
    name: "علی رضایی",
    /** Shown alongside stored session phone on the map overlay when needed */
    avatarLetter: "ع",
} as const;
